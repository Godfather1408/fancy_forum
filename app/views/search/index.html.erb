<% if params[:q] %>
  <h3>Bitte geben Sie ein wonach Sie suchen wollen.</h3>
<% else %>
  <h1>Search results</h1>
  <ul class="unstyled">
    <% @users.each do |user| %>
      <li>
        <%= link_to user.username, user %>
      </li>
    <% end %>
  </ul>
<% end %>